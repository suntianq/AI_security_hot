# Parser fixtures

Historical response samples for parser tests. Tests must NOT hit real sites —
they replay these fixtures (MVP §13/§16.1). Add one sample per parser version
when a source's format changes, so drift is caught by CI.
